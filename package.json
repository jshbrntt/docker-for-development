{
  "name": "docker-for-development",
  "version": "1.0.0",
  "repository": "https://github.com/hakimel/reveal.js.git",
  "author": "Joshua Barnett <joshua@barne.tt>",
  "license": "MIT",
  "scripts": {
    "start": "marp --bespoke.progress --html --server src",
    "build": "marp --bespoke.progress --html src -o index.html",
    "format": "prettier --write src",
    "lint": "prettier --check src"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^1.4.2",
    "prettier": "^2.4.1"
  }
}
